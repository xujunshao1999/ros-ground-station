# Transmit Configuration — saved subscriptions for startup recovery
# This file is auto-generated by the MQTT-ROS Bridge on config_sync.
# It stores the set of active topic subscriptions per robot so they
# can be restored after a bridge restart.
subscriptions:
  # robot_id:
  #   ros_topic:
  #     msg_type: "sensor_msgs/Imu"
  #     freq_limit: 10.0
